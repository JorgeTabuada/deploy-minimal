/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  
  // Configuração mínima e estável
  images: {
    domains: ['ioftqsvjqwjeprsckeym.supabase.co']
  }
};

module.exports = nextConfig;